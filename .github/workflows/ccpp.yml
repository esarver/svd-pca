name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - working-directory: build 
    - uses: actions/checkout@v1
    - name: configure
      run: cmake ..
    - name: make
      run: make
    - name: test
      run: ctest -V
